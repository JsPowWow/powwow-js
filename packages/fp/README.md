# fp

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build fp` to build the library.

## Running unit tests

Run `nx test fp` to execute the unit tests via [Vitest](https://vitest.dev/).
