# routing-utils

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build routing-utils` to build the library.

## Running unit tests

Run `nx test routing-utils` to execute the unit tests via [Vitest](https://vitest.dev/).
